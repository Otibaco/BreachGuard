import LoginForm from "@/components/admin/LoginForm";

export const metadata = {
  title: "Sign in — BreachGuard Control Center",
};

export default function AdminLoginPage() {
  return <LoginForm />;
}
